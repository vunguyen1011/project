package com.thanglong.project.domain.ENUM;

public enum InputType {
    DROPDOWN,    // Dùng cho thuộc tính cố định, người dùng chọn từ danh sách
    TEXT_INPUT   // Dùng cho thuộc tính không cố định, người dùng tự nhập
}
